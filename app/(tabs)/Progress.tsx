import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

export default function Progress() {
  return (
    <SafeAreaView>
      <Text>Progress page</Text>
      <Text>TODO: allow drop down to select sessions, and display the stats to show progress for each session.</Text>
      <Text>Should show, days user used this session, total time spent on session, and exercise stats in session</Text>

    </SafeAreaView>
  )
}